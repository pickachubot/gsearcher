{
    "name": "GsearchxBot",
    "description": "Simple Telegram Bot in Which you can search From Google and you can use it in inline too",
    "stack": "container",
    "keywords": [
      "telegram",
      "Google search"
    ],
    "website": "https://github.com/DKBOTx",
    "repository": "https://github.com/pickachubot/gsearcher",
    "env": {
        "BOT_TOKEN": {
            "description": "Your bot token.",
            "required": true
        },
        "API_ID": {
            "description": "Get this value from https://my.telegram.org",
            "required": true
        },
        "API_HASH": {
            "description": "Get this value from https://my.telegram.org",
            "required": true

        }
    },
    "addons": [],
    "buildpacks": [{
        "url": "heroku/python"
    }],
    "formation": {
        "worker": {
            "quantity": 1,
            "size": "free"
        }
    }
}
